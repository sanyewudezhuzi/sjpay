<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zhuzi.excel.ExcelDao">
    <insert id="batchSave" parameterType="org.zhuzi.excel.SysStaff">
        insert into sys_staff
        (id, username, password, name, auth, status, create_time, update_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id},
            #{item.username},
            #{item.password},
            #{item.name},
            #{item.auth},
            #{item.status},
            #{item.createTime},
            #{item.updateTime})
        </foreach>
    </insert>
</mapper>